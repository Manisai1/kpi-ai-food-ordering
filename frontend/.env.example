VITE_API_URL=http://localhost:8000/api
# Optional: only needed if you want the checkout widget to show your key
# before the backend responds (the backend always sends the correct key too).
VITE_RAZORPAY_KEY_ID=
